import login from "./login";
import register from "./register";
import refresh from "./refresh";
import validateToken from "./validate";
import revoke from "./revoke";

export default { login, register, refresh, validateToken, revoke };
